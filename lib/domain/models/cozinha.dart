import 'package:enxolist/domain/models/product.dart';

class Cozinha extends Product {
  Cozinha(
      {required super.name, required super.wasBought, required super.price});
}
//not is cool, i'll change this. Maybe, if i create an enum will be better. 